import type { Metadata } from "next";

export const metadata: Metadata = { title: "Assign items" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
